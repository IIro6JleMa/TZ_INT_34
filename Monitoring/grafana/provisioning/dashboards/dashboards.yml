apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: '' #dir gde budut dashboards
    type: file  #type of file
    disableDeletion: false #nelza udalat dashboards
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards #path for dashboards
